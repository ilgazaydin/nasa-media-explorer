# Environment Configuration Examples
# 
# This project uses environment-specific files:
# - .env.development (loaded in development mode)
# - .env.production (loaded in production mode)  
# - .env (fallback for any missing variables)
#
# Vite automatically loads the correct file based on the --mode flag

## Available Environment Variables:

# Application Mode
NODE_ENV=development|production
VITE_DEV_MODE=true|false

# API Configuration
VITE_API_BASE_URL=http://localhost:4000/api
# For production: http://node-api-env.eba-kibk2fpi.eu-west-2.elasticbeanstalk.com/api

# Authentication (Development)
VITE_MOCK_AUTH=true|false

# Application Branding
VITE_APP_TITLE=NASA Media Explorer
